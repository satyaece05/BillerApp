export class Shops {
    constructor(
        public header:{
            logo: string;
            storeName: string;
            storeAddress: string;
            storeEmail: string;
        }) {
    }
}
